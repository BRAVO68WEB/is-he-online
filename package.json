{
  "name": "@is-he-online/root",
  "version": "1.0.0",
  "description": "Real-time Discord and VSCode activity monitoring system",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "dotenv -- turbo build",
    "dev": "dotenv -- turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "@turbo/gen": "^1.12.4",
    "dotenv-cli": "^10.0.0",
    "prettier": "^3.2.5",
    "turbo": "^1.12.4",
    "typescript": "^5.0.0"
  },
  "packageManager": "yarn@1.22.22",
  "engines": {
    "node": ">=20"
  }
}
